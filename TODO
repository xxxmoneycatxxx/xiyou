# 幻想西游 优化清单

> 按投入产出比排序。安全相关暂略，避免过度设计。

---

## ✅ 已完成（P0~P2，2026-06-21）

| # | 事项 | 摘要 |
|--:|------|------|
| 1 | OPcache 调优 | 1700+ 文件免重复编译，`memory_consumption=256`, `revalidate_freq=60` |
| 2 | 静态资源缓存 | 728 张图片/CSS 加 30 天浏览器缓存头 |
| 3 | DB 连接复用 | Medoo `PDO::ATTR_PERSISTENT` |
| 4 | 关键索引 | 47 张表 `ADD INDEX idx_wjid`，`all_zt` 额外 `idx_dj`，`all_pm` 额外 `idx_pmwpfl` |
| 4.5 | MAX(id)+1 → AUTO_INCREMENT | 消除并发竞态，20+ 文件去手动自增 |
| 5 | MyISAM → InnoDB | 行级锁 + 事务，两库全量迁移 |
| 6 | PHP 7.4 → 8.2 | 1.5x 性能提升，`wrappers.php` 兼容 |
| 7 | OPcache (PHP 8.x) | 合并至 #1 |

<details>
<summary>MAX(id)+1 切换速查（已完成，供回顾）</summary>

- **模式**: `MAX(id)+1` → 删除手动 ID → INSERT 不传 ID → `lastInsertId()` 获取
- **wrappers.php**: 新增 `mysql_insert_id()` 包装 `Medoo::id()` → `PDO::lastInsertId()`
- **需 ALTER 加 AUTO_INCREMENT 的表**: `all_bp(bpid)`, `all_houres(id)`, `all_gzhoures(id)`, `all_user(id)`, 等 16 张
- **高危文件**: `xxsql.php` (玩家创建), `jlbp.php` (帮派), `gmfz.php` (住宅), `gmgz.php` (官宅+bug 修复)
- **单表切换** (~15 文件): register.php 等仅需删 MAX 查询 + 移除显式传 ID

</details>

---

## ✅ 已完成（P3，2026-06-21）

### 8. INI 文件缓存 → Redis
- **状态**: ✅ 完成并验证
- **验证结果**: 27/27 集成测试通过，19/19 API 兼容通过，168 个 Redis Key 承载玩家 10000002 全量数据
- **改造文件**: 仅 `iniclass.php`（+新增 `tests/ini_redis_verify.php`）
- **方案**: Redis Hash 优先，文件回退；`parse_ini_file`/Redis 类型归一化

---

## 🔧 进行中

### 10. PHP 8.2 Warning 清理
- **状态**: 已修复 148 处，逐批测试
- **策略**: 以 Docker 运行时 Warning 为准，IDE (Intelephense) 的跨 include 静态报告可忽略
- **模式**: `$var ??= default` / `$_POST['key'] ?? ''` / `$arr['key'] ?? default`
- **探测 Bot**: `tests/warn_scanner.php` (静态扫描) + `tests/warn_monitor.ps1` (运行时监控)
- **扫描结果**: 剩余约 9,794 处潜在警告（M001:102 / M002:2,591 / M003:6,394 / M004:9 / M005:698）

---

## ⬜ P3 第四波 ◆ 缓存层升级

### 9. Session 存储改用 Redis
- **收益**: 多进程无锁竞争，容器重建不丢失
- **风险**: 低（依赖 #8 引入 Redis）
- **工时**: 半小时
- **做法**: `session.save_handler=redis`
- **阻塞**: 硬阻塞于 #8

---

## 进度追踪

| 编号 | 事项 | 状态 |
|------|------|:--:|
| 1~7 | P0~P2 全部 | ✅ |
| 8 | INI→Redis | ✅ |
| 9 | Session→Redis | ⬜ |
| 10 | PHP 8.2 Warning 清理 | 🔧 |

---

## 剩余阻塞

```
#9 Session→Redis 无阻塞依赖（Redis 环境已由 #8 就绪）
```

- **#9 无前置阻塞**，可随时开始
